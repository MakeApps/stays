"""Channel adapters: one transport per online travel agency.

See :mod:`app.channels.base` for the interface and, more importantly, for
:class:`~app.channels.base.ChannelCapability`, which is how the rest of the
system learns what a given transport can and cannot do.
"""
